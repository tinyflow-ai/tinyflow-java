package dev.tinyflow.core.chain.repository;

public enum NodeStateField {
    INSTANCE_ID,
    STATUS,
    MESSAGE,
    ERROR,
    MEMORY,
    PAYLOAD,
    NODE_STATES,
    COMPUTE_COST,
    SUSPEND_NODE_IDS,
    SUSPEND_FOR_PARAMETERS,
    EXECUTE_RESULT,
    RETRY_COUNT, EXECUTE_COUNT, EXECUTE_EDGE_IDS, ENVIRONMENT
}
